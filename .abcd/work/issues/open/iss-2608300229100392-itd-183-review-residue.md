---
schema_version: 1
id: "iss-2608300229100392"
slug: "itd-183-review-residue"
severity: "minor"
category: "inconsistency"
source: "impl-review"
found_during: "itd-183 adversarial reviews, 2026-08-30"
found_at: "internal/core/reading/project.go, include.go, assemble.go, internal/surface/cli/reading.go"
---

itd-183 review residue: a projected field takes site.Section.Body, which ends at the next heading of any level, so a ### under a projected ## is dropped silently while the redactor uses the level-aware rule; the intent projection names Scope Conditions and Mechanism, headings no shipped record carries, so the charter's five-fields claim is not what the run does (three fields travel today); the two artefact writes are sequential os.WriteFile calls, non-atomic and overwrite-in-place; git status collapses an untracked directory so admitted files inside it are not refused; a preamble before --- lets origin through on whole-file rows (record_schema refuses such records); storeNodeType carries adr/iss entries no row uses; the --dry-run help overclaims when --out is inside the repository.
