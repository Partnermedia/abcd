---
schema_version: 1
id: "rdi-2609022244124504"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0170 (spec, spc-61) read against the assembled input this reading was handed — itm-0164 (spec, spc-55), itm-0177 (spec, spc-68) and itm-0324 (source, package cli)"
tension: "spc-61 states the bundle carries no repository path and names a test that \"scans the serialised bundle for any path separator or known repo path fragment\". The bundle in hand carries repository paths inside item text: itm-0164 names `internal/core/intent/claims.go`, `internal/core/intent/ready.go` and `internal/core/intent/lifecycle.go`; itm-0177 names `include.go` and `internal/surface/cli/regime_surface_test.go:78`; itm-0324's source text names `.abcd/config/reading-presets.json` and the import path `github.com/[redacted-user]/abcd/internal/core/reading`."
constraint_in_play: "\"projects the admitted fields into a **bundle carrying no repository path**\" and \"`TestBundleCarriesNoRepositoryPath` (scans the serialised bundle for any path separator or known repo path fragment)\" (itm-0170, spc-61)."
why_a_tension: "The pathlessness claim and the test as described are stated over the serialised bundle, not over the item envelope alone. A bundle whose item text is projected specs and Go source necessarily carries path separators and repository path fragments, so a test that scans the serialised bundle for either cannot be passing on the artefact the assembler actually produces, and the record's account of what the bundle carries does not describe what a reading receives."
---

