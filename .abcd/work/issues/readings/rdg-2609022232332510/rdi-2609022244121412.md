---
schema_version: 1
id: "rdi-2609022244121412"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0165 (spec, spc-56) read against itm-0303 (source, package lint)"
tension: "spc-56 states that `record_provenance` walks the record stores through `lint.LoadRecordGraph`. The shipped rule does not call it: `checkRecordProvenance` calls `scanRecordStores(repoRoot, scanCfg)` and states why — \"It goes to the scan rather than to the exported graph because it needs two things the graph does not carry: the frontmatter LINE a finding points at, and the raw values of keys the graph has no field for.\""
constraint_in_play: "\"`record_provenance` walks the record stores through `lint.LoadRecordGraph` (the one canonical record scan `core/site` already consumes) and reports four states\" (itm-0165, spc-56)."
why_a_tension: "The spec names the mechanism as a fact about the shipped rule, and the shipped rule takes a different entry point and records that the named one cannot carry what it needs. The single-parser property both accounts rest on is argued from different objects."
---

