---
schema_version: 1
id: "rdi-2609022244122370"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0175 (spec, spc-66) read against itm-0298 (source, package lint) and itm-0263 (source, package issueschema)"
tension: "spc-66 records that `record_schema` gains a schema for the reading and disposition shapes when spc-58's stores land. The stores landed; the schemas did not. The shipped `recordStores` entries for `rdi`, `rdg` and `dsp` declare `bucketRe`, `fileNumRe`, `fileFamily` and `filename` and no `requiredFields` or `knownFields`, and the code states this in its own comment: \"Their required-field sets are absent, and that is a STATED GAP rather than a delegation … a record hand-written into these trees can carry a body no reader reads and pass this gate.\" The `adm` and `srp` entries beside them do declare their sets from `issueschema`."
constraint_in_play: "\"The validation path for those shapes is spc-58's own, and it arrives with spc-58: until its reading and disposition stores land, `record_schema` has no schema for either shape and refuses their directory as an undeclared bucket … Until then the shapes are held by this definition and by whoever reviews the commit.\" and \"The transcribed material is committed through the ordinary record path, which on this base carries no schema for either shape: `record_schema` gains one with spc-58's stores\" (itm-0175, spc-66)."
why_a_tension: "spc-66 makes the scribe's hand-written records safe by pointing at a committed-tree schema arriving with spc-58's stores. The stores exist in the shipped gate and carry no schema, so the protection spc-66 defers to is not in the tree, and the shipped comment says so in the same words the spec uses to promise it."
---

