---
schema_version: 1
id: "rdi-2609022244123883"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0161 (intent-projection, itd-199 acceptance criteria) and itm-0178 (spec, spc-69) read against itm-0324 (source, package cli)"
tension: "The shipped `assemble` verb takes two operands. It requires `--position` and `--target` and nothing else; the flag-error handler states \"The invocation is --position and --target, and nothing else\" and describes `--scope` as \"an operand this verb used to take — `--scope`, withdrawn by adr-2609021016286571\". The shipped intent itd-199 and the closed spec spc-69 that delivers it both require a scope operand and a refusal when it is absent."
constraint_in_play: "\"- **Given** an invocation with no scope, **when** it runs, **then** the verb refuses and names the closed grammar, as it already does for an absent position.\" (itm-0161, itd-199's acceptance criteria); \"`abcd reading assemble` takes a third operand naming what the reading is about\" (itm-0178, spc-69)."
why_a_tension: "itd-199 stands in the record as a shipped intent whose criteria are the bar for \"shipped\", and spc-69 is its closed design record. The delivered verb cannot satisfy a criterion that requires it to refuse an invocation carrying no scope, because it registers no such operand and refuses one if handed."
---

