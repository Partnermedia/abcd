---
schema_version: 1
id: "rdi-2609022244129894"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0172 (spec, spc-63) read against itm-0324 (source, package cli) and itm-0181 (doc, commands/reading.md)"
tension: "The spec that owns the ingest verb names its operand `--output-json`, and names a surface test `TestIngestRequiresOutputJSON` that pins it. The shipped front door registers `--reading-json` instead: the command's `Use` string reads `ingest --reading-json <path>`, the flag is declared as `ingestCmd.Flags().StringVar(&readingJSON, \"reading-json\", \"\", \"path to the JSON the cold reading returned\")`, and the refusal reads `reading ingest: --reading-json <path> is required`. The delivered plugin page documents `--reading-json` as well."
constraint_in_play: "\"The verb is `abcd reading ingest --output-json <path>` in the same `internal/core/reading` package as the assembler\" — and, in its Tests section, \"`internal/surface/cli/reading_surface_test.go`: `TestIngestRequiresOutputJSON`\" (itm-0172, spc-63)."
why_a_tension: "The spec is the design record spc-63 was built against and states the operand as a fact about the verb, not as a proposal; the shipped verb accepts a differently named operand and refuses the one the spec names. A reader following the spec's invocation gets a refusal, and the test the spec names as the pin cannot be the test that pins what ships."
---

