---
schema_version: 1
id: "rdi-2609022244125170"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0161 (intent-projection, itd-199 acceptance criteria) and itm-0178 (spec, spc-69) read against itm-0324 (source, package cli)"
tension: "The shipped result carries no override stamp and no scope source. `renderPreset`'s header states \"There is no source token and no override note, because there is no operand: the entry follows from the position\", and the plugin page repeats \"There is **no** override stamp and no scope source\". itd-199 requires the manifest to stamp a run as overridden when its scope departs from a committed preset, and spc-69's acceptance mapping delivers that as `Manifest.ScopeOverridden`."
constraint_in_play: "\"- **Given** a run whose scope names a record id or a record family directly rather than a committed preset, **when** its manifest is read, **then** the run is stamped as overridden; **and given** a run whose scope names a committed preset, **then** it is not. … drift between what is committed and what people actually run is countable rather than invisible.\" (itm-0161, itd-199)."
why_a_tension: "The criterion's purpose — counting departures from the reviewed configuration — is stated as delivered in a shipped intent, and spc-69 records the field that delivers it. The shipped surface removes both the departure and its counter, so the record asserts a countable property the tree no longer carries any field for."
---

