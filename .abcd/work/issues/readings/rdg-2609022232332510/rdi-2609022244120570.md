---
schema_version: 1
id: "rdi-2609022244120570"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0142 (intent-projection, itd-186 acceptance criteria) and itm-0173 (spec, spc-64) read against itm-0194 and itm-0204 (source, package capture) and itm-0261 (source, package issueschema)"
tension: "The shipped tree reads the instrument's own prior output back into an assembly. `capture.ValidateReadingRecord`'s header states \"The cold-reading assembler admits one widening run's records as a comparative reading's candidate set (adr-2609021016272867)\"; `itemfate.go` exists so \"the comparative assembly refuses rather than handing a reading the researcher's judgement dressed as cold text\"; and `LedgerDirs` is derived so \"The cold-reading assembler asserts, at the comparative position, that every ledger family except the candidate run stayed behind\". itd-186's criterion and spc-64's ac-4 record that no prior-run manifest or reading record reaches the assembler's output."
constraint_in_play: "\"- **Given** a repository state containing manifests and reading records from prior runs, **when** the eval runs, **then** none of them appears in the assembler's output — the instrument's own exhaust is tested against its own read-block (added 2026-08-28; nothing else tests it).\" (itm-0142, itd-186); \"What the include table does not name is absent … including the instrument's own output.\" (itm-0170, spc-61)."
why_a_tension: "A shipped intent's criterion states, without positional qualification, that prior reading records never appear in the assembler's output, and names its eval as the only thing testing it. The shipped ledger code exists specifically to let one widening run's reading records travel into a comparative assembly, so the property the criterion asserts is no longer a property of the tree at every position."
---

