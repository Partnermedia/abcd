---
schema_version: 1
id: "rdi-2609022244129975"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0036 (brief-section, `/abcd:reading` surface) read against itm-0181 (doc, commands/reading.md) and itm-0324 (source, package cli)"
tension: "The delivered plugin page's `argument-hint` advertises three position tokens — `assemble --position <widening|entailment|detection>` — while the body of the same page states \"`--position` takes one of four closed tokens — `widening`, `entailment`, `comparative`, `detection`\", the brief's operand table names four, and the shipped flag help composes the token list from `reading.Positions()`."
constraint_in_play: "\"| `--position` | one of `widening`, `entailment`, `comparative`, `detection` |\" (itm-0036, the `/abcd:reading` surface's operand table)."
why_a_tension: "The argument-hint is the line a host reads to learn the verb's grammar, and it omits a token the same page, the brief and the binary all accept — so the delivered documentation describes a narrower invocation than the one that ships."
---

