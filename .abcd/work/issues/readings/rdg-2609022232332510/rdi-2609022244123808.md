---
schema_version: 1
id: "rdi-2609022244123808"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0166 (spec, spc-57) read against itm-0226 (source, package grounds), itm-0260 (source, package issueschema) and itm-0298 (source, package lint)"
tension: "The shipped grounds package describes a carrier the record retired. `Parse`'s doc comment reads \"Parse reads the `<token>: <text>` grammar — the one spelling written to a `grounds:` frontmatter scalar and to a `- <token>: <text>` bullet alike\", and `New`'s comment adds \"both carriers hold a single line\". In the same tree, `issueschema.Known` records `grounds` as a legacy key \"no longer written … the reader ignores their values entirely\", and the record lint reports a frontmatter `grounds:` with \"a frontmatter value is read by nothing\"."
constraint_in_play: "\"`grounds` TOLERATED in `Known` as a legacy key a record written by an older abcd may carry, its value read by nothing; the record lint's `record_schema` blocks a frontmatter `grounds:` and names the section it belongs in.\" (itm-0166, spc-57)."
why_a_tension: "The package comment states, as present behaviour, that the grammar is written to a frontmatter scalar; the spec that moved grounds into the record body records that no write path produces one and that the committed-tree gate blocks it. The comment describes a writer that the same tree refuses."
---

