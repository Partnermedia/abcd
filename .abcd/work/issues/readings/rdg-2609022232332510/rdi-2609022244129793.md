---
schema_version: 1
id: "rdi-2609022244129793"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0138 (intent-projection, itd-185 acceptance criteria) read against itm-0263 (source, package issueschema) and itm-0204 (source, package capture)"
tension: "itd-185's criterion names the provenance field `pattern_named`. The shipped envelope field is `pattern`: `issueschema.ReadingRequired` is `{\"schema_version\", \"id\", \"run\", \"manifest\", \"position\", \"regime\", \"pattern\"}`, and `readingFields` writes `{\"pattern\", item.Pattern}`. No field named `pattern_named` exists anywhere in the shipped record schema or writer."
constraint_in_play: "\"- **Given** an item at any of the four regimes whose `pattern_named` envelope field is empty or absent, **when** it is ingested, **then** ingest refuses that item, without exception at any regime.\" (itm-0138, itd-185)."
why_a_tension: "The criterion is stated over a named field, and the shipped tree carries no field of that name; spc-63 records the two names as \"one requirement\" whose \"wire name is `pattern`\", which resolves the build but leaves the shipped intent's criterion naming something no reader of the tree can locate."
---

