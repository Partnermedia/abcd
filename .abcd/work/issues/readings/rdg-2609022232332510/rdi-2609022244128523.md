---
schema_version: 1
id: "rdi-2609022244128523"
run: "rdg-2609022232332510"
manifest: "2b7e4b5401f5e9d553fa70fe9bac0df7d5952d2e25867881fc052cba22d5ffc2"
position: "detection"
regime: "registrative"
pattern: "itm-0161 (intent-projection, itd-199 acceptance criteria), itm-0173 (spec, spc-64) read against itm-0324 (source, package cli) and itm-0181 (doc, commands/reading.md)"
tension: "The shipped assembler assembles at the comparative position. Its `--position` flag help reads \"comparative derives its candidate set from the record: the one committed widening run at the target whose items carry no disposition and no admission\"; `renderAssembleResult` prints \"candidate run: %s (%d candidate(s), derived from the record)\"; and the not-exercised path stages a run whose ingest \"commit[s] a comparative run with an empty item set naming it\". itd-199's criterion and spc-64 both record that the position refuses and writes nothing."
constraint_in_play: "\"- **Given** an invocation at the comparative position, **when** it runs, **then** the verb refuses and names the channel it lacks, rather than assembling a bundle identical to another position's and reporting success.\" (itm-0161, itd-199); \"Holding that position is `TestComparativeRefusesToAssemble` … which asserts the refusal itself and asserts that neither bundle nor manifest is written\" (itm-0173, spc-64)."
why_a_tension: "Two shipped records state that the comparative position does not assemble at all and that an eval holds it to that; the shipped verb derives a candidate set and stages artefacts at that position. Both cannot describe the same tree, and spc-64's named eval asserts the absence of exactly what the shipped verb now produces."
---

