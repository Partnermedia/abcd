---
schema_version: 1
id: "rdi-2609022215133863"
run: "rdg-2609022207359936"
manifest: "28a9613e875be70f2bdce804e63730a2fa16ac4d0dfc857da67fa702f93723af"
position: "entailment"
regime: "explicative"
pattern: "output vocabulary inherited from the authored input vocabulary"
claim_surfaced: "The entailment position's item body is fixed to the three claim kinds an intent is authored in — criterion, mechanism/causal, context — and a fourth claim type is explicitly not ruled. A commitment the design carries that fits none of the three has no shape in which it can be returned, so the position's output space is bounded by the record's own authoring template rather than by what a design can commit to."
claim_type: "context"
what_implies_it: "spc-63's body table ('`explicative` | `claim_surfaced`, `claim_type` (`criterion` / `causal` / `context`), `what_implies_it`'), discipline itd-190's gradient table (criterion / mechanism / context), and the scope condition on the projected intent delivered by spc-2609020626042471 ('The claim vocabulary is the three claim kinds intents carry, in the design documents' words. A fourth claim type is a ruling this intent does not make')."
---

