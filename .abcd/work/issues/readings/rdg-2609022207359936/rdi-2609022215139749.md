---
schema_version: 1
id: "rdi-2609022215139749"
run: "rdg-2609022207359936"
manifest: "28a9613e875be70f2bdce804e63730a2fa16ac4d0dfc857da67fa702f93723af"
position: "entailment"
regime: "explicative"
pattern: "minted, gated, but unresolvable identifiers"
claim_surfaced: "Reading items, dispositions, admissions and surprises are minted through the shared record-id seam, filed in declared record families and held by the record gate, while the cited-id grammar the repository's own dispatch and citation surfaces use covers only four families. The design commits to a class of record whose ids are written into other records and cannot be resolved by the resolvers that read those records."
claim_type: "criterion"
what_implies_it: "spc-58's out-of-scope ('Dispatching `abcd <id>` on the new families. `recordid.CitedIDRe` covers the four id-bearing families only'), spc-67's matching residual, the projected intent delivered by spc-2609020626040342 ('`abcd <id>` dispatches on `adm-N` and `srp-N`, which no record dispatch reaches today'), and the core `record` glossary entry, whose definition already counts 'an itd-N, spc-N, adr-N, iss-N or rdg-id'."
---

