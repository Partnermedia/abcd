---
schema_version: 1
id: "rdi-2609022215139432"
run: "rdg-2609022207359936"
manifest: "28a9613e875be70f2bdce804e63730a2fa16ac4d0dfc857da67fa702f93723af"
position: "entailment"
regime: "explicative"
pattern: "self-attested field inside an attestation"
claim_surfaced: "Instrument identity is `model`, `definition_sha256` and `assembler_version`; the verb recomputes the definition hash and compares the assembler version against the manifest, but the model name is taken from the payload and checked against nothing. The committed run record therefore attests a model the examination behind it never established."
claim_type: "criterion"
what_implies_it: "spc-63's 'Manifest reference' section ('Instrument identity ... is `model` plus `definition_sha256` plus `assembler_version`, all three required ... The definition hash is recomputed here from the definition file and compared with the payload's claim; the assembler version is compared with the manifest's'), read against brief invariant 16 ('an attestation ... never states more than the examination behind it establishes')."
---

