---
schema_version: 1
id: "rdi-2609022215134208"
run: "rdg-2609022207359936"
manifest: "28a9613e875be70f2bdce804e63730a2fa16ac4d0dfc857da67fa702f93723af"
position: "entailment"
regime: "explicative"
pattern: "the instrument cannot be its own object"
claim_surfaced: "The detection position's object is the shipped tree against the claim record, but the include table denies `agents/`, `evals/` and `internal/core/reading/` besides the whole `.abcd/` namespace. The cold-reading instrument — its definitions, its assembler and its evals — is the one part of the shipped tree that no detection reading can ever be handed."
claim_type: "context"
what_implies_it: "spc-61's 'Default deny, and the two assembler rules' ('\"The shipped tree\" is scoped to source, tests, documentation and configuration, with `.abcd/` denied wholesale and `agents/`, `evals/` and `internal/core/reading/` denied besides'), read against the detection definition's object as stated in spc-62's table ('The shipped tree against the claim record')."
---

