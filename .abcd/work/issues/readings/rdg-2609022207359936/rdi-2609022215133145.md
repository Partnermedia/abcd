---
schema_version: 1
id: "rdi-2609022215133145"
run: "rdg-2609022207359936"
manifest: "28a9613e875be70f2bdce804e63730a2fa16ac4d0dfc857da67fa702f93723af"
position: "entailment"
regime: "explicative"
pattern: "an advisory-only gate: severity pinned below blocking"
claim_surfaced: "Loop closure is never gated. The outstanding report is pinned to `info` in code precisely so no configuration can raise it, so a run whose items are never answered — and a widening proposal that is neither admitted nor declined — leaves every check green indefinitely."
claim_type: "criterion"
what_implies_it: "spc-58's 'The outstanding report is a report' ('Its severity is pinned to `info` in code, not read from `RuleConfig`: a rule whose severity a config could raise to blocker is a gate waiting to happen') and spc-67's admission leg ('Its severity stays `info` and stays pinned in code for spc-58's reason: an unanswered proposal must never block an unrelated push')."
---

