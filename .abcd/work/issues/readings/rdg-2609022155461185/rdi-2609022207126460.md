---
schema_version: 1
id: "rdi-2609022207126460"
run: "rdg-2609022155461185"
manifest: "fc868379def4f004ff2fecfc0a1d7b46b2959b5c72a3d67e241d1d74ea8cebea"
position: "widening"
regime: "generative"
pattern: "a lapse recorded as a category with an instant"
configuration: "A lapse record joined to the discipline that gave way, so a discipline's own record carries the occasions on which it was not held."
what_admits_it: "The issue schema as passed makes `lapsed_at` required when the category is `lapse` and declares no join from a lapse to any other record, while the record-schema store table declares joins for the admission (`proposal`) and the surprise (`occasioned_by`) alone; the intents store declares a `disciplines` bucket."
---

