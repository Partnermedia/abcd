---
schema_version: 1
id: "rdi-2609022207129380"
run: "rdg-2609022155461185"
manifest: "fc868379def4f004ff2fecfc0a1d7b46b2959b5c72a3d67e241d1d74ea8cebea"
position: "widening"
regime: "generative"
pattern: "a report pinned in code never to gate"
configuration: "A release-time arming of the outstanding-readings report, on the caller-supplied promotion shape the same package already carries for two other rules, so a cut cannot be taken while reading items stand unanswered."
what_admits_it: "`severityPinnedRules` pins `reading_outstanding` to `info` in code, with the comment \"a rule whose severity a config could raise to blocker is a gate waiting to happen\"; the same passed package carries `ArmReceiptGate` and `ArmCitationOverdue`, both of which promote a severity from the caller at release time."
---

