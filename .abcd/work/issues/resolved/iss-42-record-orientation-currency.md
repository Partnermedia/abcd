---
schema_version: 1
id: "iss-42"
slug: "record-orientation-currency"
severity: "major"
category: "drift"
source: "agent-finding"
found_during: "2026-07-08 multi-agent review"
found_at: ".abcd/work/CONTEXT.md"
resolution: "CONTEXT.md's sharp-edges list gains a detector, not just a correction: the new context_citation_currency record-lint rule resolves every iss-N/itd-N/spc-N/adr-N handle in the Live constraints section against its store and blocks a citation to a terminal lifecycle state (resolved/wontfix, shipped/superseded, closed, or an ADR's declared supersession or retired status), so the class recurs into a red gate rather than into stale orientation. The four named instances are corrected: the trust caveat now states which half of the brief-surface agreement is machine-enforced (surface_coverage, Direction B, always-on) and which is the periodic release-gate crosscheck (Direction A, pinned and tiered by iss-122), keeping the practical instruction to trust the binary; the development record map routes research/ to its real children notes/ + prompting/ instead of the phantom spikes/; and five dated notes move from research/ into research/notes/ where their ~25 peers already sit, with every citation followed. The record map row is GATED rather than merely corrected (iss-38 precedent): an index_drift region holds it to research/'s actual subdirectories, so any spelling of the phantom child fails the gate, and a banned_tokens entry (research-note-outside-notes) catches a dated note cited at research/ root. The producer of that second class is fixed too: abcd ideate record now writes its dated verdict record into research/notes/ rather than research/ root."
impact: additive
---

record orientation currency: CONTEXT.md is stale on the record trust caveat — its single most important orientation fact; phase-shift updates are not a required handoff step so the staleness recurs structurally; record README routing claims point at moved locations; research/ contradicts its own naming convention. Detector: a handoff checklist rule (a phase shift or trust-caveat change requires a CONTEXT.md update in the same change) plus banlist coverage for the moved routing targets. Acceptance corpus: the four instances above.