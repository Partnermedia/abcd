---
schema_version: 1
id: "iss-267"
slug: "hook-sub-verb-names-are-now-a-compatibility-contract-not-a-c"
severity: "minor"
category: "observation"
source: "user-observation"
found_during: "manual-capture"
resolution: "The unknown-SUB-VERB path under the two parents a host hook reaches (guard, hook) now refuses at exit 1 rather than cobra usage status 2, via failOpenNoArgs. On the hook plane 2 is the host BLOCKING status, so refusing there made abcd block every shell command and every prompt on a skewed manifest/binary pair while claiming a verdict it never reached -- violating the fail-open-loud contract (spc-16, itd-103 AC 1) that reserves 2 for a real decision to block. The refusal stays loud and non-zero, so iss-266 guarantee is intact; only the code moves. SCOPE: this covers unknown sub-verbs under those two parents only. A stray positional on a leaf, an unknown flag (FlagErrorFunc, untouched) and an unknown top-level token all still exit 2; none is reachable from today manifest, and iss-269 carries that gap. Root cause of the override: markUsageErrorsExitTwo re-stamped EVERY validator error as exit 2, discarding a code a validator had chosen deliberately; it now preserves an existing exitError. TestHooksManifestNamesLiveSubverbs decodes hooks/hooks.json and resolves every invocation against the live command tree, scanning each command separately so a quoting drift that hides one invocation cannot leave it unpinned. It matches through ALIASES, which is load-bearing: the skew runs manifest-ahead-of-binary (clone pulls, binary lags), so the only remedy that survives is freezing the manifest spellings and absorbing renames as aliases in the binary -- a name-equality check would have rejected exactly that remedy. Mutation-verified in four directions: rename without alias fails, rename WITH alias passes, a quoting drift hiding one invocation fails, a manifest naming a prefix fails. Verified end-to-end through the built binary and through the real wrapper strings: a renamed sub-verb exits 1 on both planes, a genuine hazard still exits 2, an allow still exits 0."
impact: fix
---

Hook sub-verb names are now a compatibility contract, not a cushion. iss-266 makes every parent runnable, so an unknown sub-verb exits 2 instead of printing help at exit 0. On the Claude Code hook plane exit 2 is the BLOCKING status: exit 2 on UserPromptSubmit blocks the prompt, and exit 2 from 'guard hook' on PreToolUse blocks every Bash tool call -- and the PreToolUse wrapper in hooks/hooks.json treats 2 as a recognised code, so its 'FAILED TO RUN ... UNGUARDED' safety net does not fire. hooks.json ships with the plugin git clone while the binary is fetched from the latest release by hooks/bootstrap.sh, so the two can skew. No live instance today (every sub-verb hooks.json names exists at v0.5.1), and the fail-closed direction is intended. But a future rename of 'guard hook' or any 'hook <sub-verb>' must now carry an alias rather than rely on the old silent exit-0 cushion: against a skewed hooks.json/binary pair it would brick a session where it previously degraded silently. Found by the security review of the iss-266 fix.