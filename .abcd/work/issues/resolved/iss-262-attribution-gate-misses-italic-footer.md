---
schema_version: 1
id: "iss-262"
slug: "attribution-gate-misses-italic-footer"
severity: "minor"
category: "security"
source: "impl-review"
found_during: "spc-26 build, attribution CI triage"
found_at: "scripts/check-attribution.sh"
resolution: "GENERATED_RE now admits an optional attached markdown emphasis run ([_*]{1,2}) before the Generated with/by shape, so the italic footer form a hosted agent platform appends is refused by the body check. The marker must be attached to the word, matching markdown's own reading (emphasis vs list item), which keeps a bullet describing the banned footer passing as documentation -- the same writable-about property the line anchor protects. Five corpus cases pin the emphasis forms (underscore, asterisk, bold, emoji, after a horizontal rule) and two pin the list-item forms that must keep passing; all five were watched fail before the change. Verified against the exact footer string observed live on PR 293."
impact: fix
---

check-attribution.sh's GENERATED_RE misses the italic footer form a hosted agent platform actually appends: '_Generated by [Claude Code](...)_' starts with an underscore, and the regex anchors '^[[:space:]]*(robot)?[Gg]enerated (with|by) [' — so the exact footer shape that motivated the gate sails through the body check when wrapped in markdown italics. Admit an optional leading '_' or '*' (and matching trailing) in the shape. Found live: both walkability PRs carried the italic footer and the body check did not flag it; the failing leg was commit identity.